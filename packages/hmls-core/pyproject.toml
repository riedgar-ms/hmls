[project]
name = "hmls-core"
version = "0.1.0"
description = "Core game types for HMLS tank game"
requires-python = ">=3.14"
dependencies = [
    "pydantic>=2",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/hmls"]
